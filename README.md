# urlshortener
Simple urlshortener written on flask.

Type commands:
$ cd urlshortener
$ python 
$ >>> from server import db
$ >>> db.create_all()
$ >>> exit()
$ python server.py 

Now, when db is created, you able to use it.
